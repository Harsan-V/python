print("Longest substring without repeating characters")

s = "abcabcbb"
left = 0
seen = set()
max_len = 0
for right in range(len(s)):
    while s[right] in seen:
        seen.remove(s[left])
        left -= 1
    seen.add(s[right])
print(seen)
