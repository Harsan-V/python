class Solution(object):
    def containsNearbyDuplicate(self, nums, k):
        seen = {}  # value -> latest index
        for i, num in range(len(nums)):
            if num in seen and i - seen[num] <= k:
                return True
            seen[num] = i        
        return False


        
